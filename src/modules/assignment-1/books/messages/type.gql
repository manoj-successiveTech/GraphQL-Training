type Message {
  id: ID!
  content: String!
  author: User!
  title: String
  createdAt: String
}

extend type Query {
  messages: [Message!]!
}

extend type Mutation {
  postMessage(content: String!, authorId: ID!, title: String): Message!
}
