# src/modules/type.gql

type User {
  _id: ID!
  name: String!
  email: String!
  posts: [Post!]!
}

type Post {
  id: ID!
  title: String!
  content: String!
  authorId: ID! # Relationship, not authorId
  comments: [Comment!]!
  createdAt: String
}

type Comment {
  id: ID!
  text: String!
  author: User!
  postId: String!
}

type Message {
  id: ID!
  content: String!
  author: User!
  title: String
  createdAt: String
}

# custom error(ass-2.5)
type Error{
  code: Int!
  message: String!
}

type LoginResult{
  token:String!
  user:User
}